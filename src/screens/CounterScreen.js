import React from "react";
import {Text, View, StyleSheet, Button} from "react-native";

const CounterScreen = () => {
    return <Text>Counter Screen</Text>
}

const styles = StyleSheet.create({})

export default CounterScreen