import React from "react";
import {Text, View, StyleSheet} from "react-native";

const ImageDetails = () => {
    return <Text>Show image of animals</Text>
}

const styles = StyleSheet.create({})

export default ImageDetails
